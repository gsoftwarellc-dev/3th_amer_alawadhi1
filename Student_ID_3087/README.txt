========================================================================
 Vehicle Maintenance Service Booking System
 Student ID: 3087
========================================================================

HOW TO RUN
------------------------------------------------------------------------
1) Open a terminal inside the "Website"s folder only .

2) Run:

     ./start.sh

   This installs anything missing (Docker, .NET SDK), sets up SQL Server
   and the database, and launches the website. First run takes a few
   minutes; later runs are fast.

3) browser will opens automatically once the site is ready.

